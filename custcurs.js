function custcurs (curimg){
  let curtxt = curimg + ", auto";
  // console.log(curtxt);
      // document.getElementById("defaultCanvas0").style.cursor = "pointer";
      document.getElementById("defaultCanvas0").style.cursor = curtxt;

}
